[mysqld]
innodb_buffer_pool_size = 64M    # Reduce buffer pool for small datasets
innodb_log_file_size = 32M       # Smaller log files
table_open_cache = 200           # Fewer open tables
query_cache_size = 0             # Disable query cache (deprecated in MySQL 8)
max_connections = 50             # Limit connections for local testing